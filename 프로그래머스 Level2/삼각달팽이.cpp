#include <string>
#include <vector>

using namespace std;

vector<int> solution(int n) {
    vector<int> answer;
    //맨 위에랑 맨 아래 분리
    /*
      1          1     1      
     26        23
     345
    */
    vector<vector<int>> triangle(n); //n개의 행 생성
    for (int i = 0; i < n; i++) {
        
    }

    return answer;
}